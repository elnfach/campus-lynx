import { Home } from "@/app/modules/Home";
import type {Route} from "@/app/core/types";

const routes: Route[] = [
    {
        path: '/',
        element: Home,
        layout: ,
    },
];

export default routes;